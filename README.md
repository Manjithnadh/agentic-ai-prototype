we added every thing like now the prototype can take the multiple files in the rag and it is using dynamic prompts to route the tools and every thing is clear 

just download the file and in terminal past this command "pip install -r requirements.txt"
this will install all the requiremnts into your machine 
after that add the openai key in the .env you need to create the .env first and add the key like use "OPENAI_API_KEY= YOUR API KEY HERE"
and run using this command streamlit run app.py

